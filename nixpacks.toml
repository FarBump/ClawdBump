[phases.setup]
nixPkgs = ["nodejs_22", "corepack"]

[phases.install]
cmds = ["corepack enable", "corepack prepare pnpm@10.23.0 --activate", "pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["pnpm build"]

[start]
cmd = "node dist/entry.js gateway run --port ${PORT:-18789}"

