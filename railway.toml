[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/entry.js gateway run --port ${PORT:-18789} --bind 0.0.0.0"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10


